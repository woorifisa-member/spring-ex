#spring:
#  datasource:
#    url: jdbc:h2:~/test
#    driverClassName: org.h2.Driver
#    username: sa
#    password:
#
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

---

spring: # local DB환경
  config:
    activate:
      on-profile: "local"
  datasource:
    url: jdbc:h2:~/test
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
    show-sql: true

    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console

---

spring: #prod DB환경
  config:
    activate:
      on-profile: "prod"

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://spring-board-ex.cdryf41hmarv.ap-northeast-2.rds.amazonaws.com:3306/testdb
    username: admin
    password: iwascar123!

  jpa:
    hibernate:
      ddl-auto: none

    #    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    properties:
      hibernate:
        format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
    show-sql: true
